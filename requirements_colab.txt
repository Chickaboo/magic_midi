# Install with: pip install -r requirements_colab.txt
# Note: mamba-ssm requires CUDA and will fail on CPU-only machines.
# The codebase automatically falls back to a GRU approximation when unavailable.
torch>=2.1.0
miditok>=3.0.0
pretty_midi>=0.2.10
ncps>=0.0.7
numpy>=1.24.0
tqdm>=4.65.0
matplotlib>=3.7.0
wandb>=0.15.0
safetensors>=0.4.0
mamba-ssm>=1.2.0
causal-conv1d>=1.2.0
